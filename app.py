# -*- coding: utf-8 -*-
"""app

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1K2SRQP5a1hmE9YmH3YFMxpjItufREvUR
"""
import pandas as pd
import numpy as np
import joblib


from flask import Flask, render_template, request
#from sklearn.externals import joblib

app = Flask(__name__)

# Load the trained model
model = joblib.load('price_predictor_model.pkl')

@app.route('/')
def home():
    return render_template('index.html')

@app.route('/predict', methods=['POST'])
def predict():
    if request.method == 'POST':
        # Get input values from the form
        input_features = [float(x) for x in request.form.values()]

        # Make a prediction
        predicted_price = model.predict([input_features])

        return render_template('index.html', prediction_text=f'Predicted Price: ${predicted_price[0]:.2f}')

if __name__ == '__main__':
    app.run(debug=True)